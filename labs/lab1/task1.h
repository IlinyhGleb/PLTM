#ifndef TASK1_H    /* This is an "include guard" */
#define TASK1_H    /* prevents the file from being included twice. */
                  /* Including a header file twice causes all kinds */
                  /* of interesting problems.*/

#include <stdio.h>
#include <stdlib.h>

int task1(int a, int b);

#endif /* TASK1_H */
