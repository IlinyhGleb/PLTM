#ifndef TASK2_H    /* This is an "include guard" */
#define TASK2_H    /* prevents the file from being included twice. */
                  /* Including a header file twice causes all kinds */
                  /* of interesting problems.*/

#include <stdio.h>
#include <stdlib.h>

float task2(float a, float b, float c);

#endif /* TASK2_H */
